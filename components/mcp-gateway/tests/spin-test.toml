# Spin Test Configuration for MCP Gateway

[test-environment]
# Core settings
component_names = "echo,calculator,weather"
validate_arguments = "true"

# Mock tool configurations
echo_url = "http://echo.spin.internal/"
calculator_url = "http://calculator.spin.internal/"
weather_url = "http://weather.spin.internal/"