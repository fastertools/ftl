{
  "name": "javascript-test-handler",
  "version": "0.1.0",
  "type": "module",
  "description": "An MCP tool written in JavaScript",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run build:types && npm run build:component",
    "build:types": "jco types wit/mcp.wit -o src/generated",
    "build:component": "jco componentize src/index.js --wit wit/mcp.wit --world-name mcp-handler --out ../handler.wasm --disable all",
    "test": "vitest",
    "clean": "rm -rf generated ../handler.wasm"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.8.1",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}