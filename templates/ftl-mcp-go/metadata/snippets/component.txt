[[trigger.http]]
route = { private = true }
component = "{{project-name | kebab_case}}"

[component.{{project-name | kebab_case}}]
source = "{{project-name | kebab_case}}/main.wasm"
allowed_outbound_hosts = []
[component.{{project-name | kebab_case}}.build]
command = "tinygo build -target=wasip1 -gc=leaking -buildmode=c-shared -no-debug -o main.wasm ."
watch = ["*.go", "go.mod"]

# IMPORTANT: After adding this component, update the component_names variable
# in the [variables] section at the top of spin.toml to include "{{project-name | kebab_case}}"
# Example: component_names = { default = "existing-tool,{{project-name | kebab_case}}" }